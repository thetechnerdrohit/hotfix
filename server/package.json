{
  "name": "hotfix-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "HOTFIX Phase 5 — authoritative Colyseus TDM server. Reuses the client game modules headlessly (see loader.mjs).",
  "scripts": {
    "start": "node --experimental-loader ./loader.mjs index.js",
    "test": "node --experimental-loader ./loader.mjs test/sim.test.mjs"
  },
  "dependencies": {
    "@colyseus/core": "^0.15.0",
    "@colyseus/schema": "^2.0.0",
    "@colyseus/ws-transport": "^0.15.0",
    "colyseus": "^0.15.0",
    "three": "^0.170.0"
  }
}
